[build]
  command = "npm run build"
  publish = ".next"
  environment = { SECRETS_SCAN_OMIT_KEYS = "SITE_URL,ADMIN_EMAIL,SMTP_HOST,SMTP_PORT,SMTP_SECURE,SMTP_FROM,SMTP_TO,SMTP_USER" }

[[plugins]]
  package = "@netlify/plugin-nextjs"
